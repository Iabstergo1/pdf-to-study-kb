# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Secrets
.env
.env.*
!.env.example
# 非 .env 的密钥泄露向量（PowerShell $PROFILE、私钥、凭据转储等）
*profile.ps1
Microsoft.PowerShell_profile.ps1
.envrc
secrets.json
*.pem
*.key
*.pfx

# Local book workspaces and generated assets
books/*
!books/.gitkeep

# Generated knowledge-base vault + local pipeline runtime state
# (state db / staging / snapshots / query-sessions / reports; spec §3.3: snapshots 默认非 git)
# 若想把知识库纳入版本控制，删掉 wiki/ 这一行即可。
wiki/
pipeline-workspace/

# Local binary tooling (llama.cpp build, large; not for VCS)
tools/

# Temporary files
tmp/
*.tmp
*.log

# Claude Code per-machine local overrides (personal defaultMode/permissions; not shared)
.claude/settings.local.json

# OS/editor
.DS_Store
Thumbs.db

.codegraph/

# MinerU models moved off C: (2.4GB; re-download via modelscope if deleted)
.mineru-models/

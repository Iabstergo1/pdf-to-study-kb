---
type: lesson
title: <小节标题>
domain: <domain>
source: <source_id>
section: "<5.2>"
status: proposed
managed_by: pipeline
---
<干净散文正文：把这一小节的知识讲成读者能直接用的东西——是什么、为什么重要、怎么用。
写成独立知识，让读者意识不到原文档存在：不写"本章/本书/作者在第 X 章"这类元叙事，不按原书章节命名（D-2）。
公式用原生 KaTeX 并写出结论、变量含义与适用条件；源图只作阅读证据、绝不嵌入正文（D-1）；不使用脚注机制（D-5），
溯源写在 frontmatter 的 source_refs。>

例：在分离均衡下，不同类型的发送者选择不同信号，接收者据此可唯一推断类型；这要求信号成本随类型递增，
否则低类型会模仿高类型、分离便无法维持。

$$u_i(s) = \sum_j p_j \cdot v_i(s_j)$$

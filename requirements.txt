# PDF to Study KB 依赖
# 架构见 CLAUDE.md（Claude Code）/ AGENTS.md（Codex）
#
# 安装（建议专用 conda 环境，勿用共享环境）：
#   conda create -y -n study-kb python=3.12
#   conda activate study-kb
#   pip install -r requirements.txt

# ── 核心确定性 CLI（零 LLM 必需）──────────────────────────────
pymupdf>=1.23.0      # PDF 解析 / 页渲染（source-convert 文本后端 + 难页 PNG）
pyyaml>=6.0          # YAML（registry / workorder / frontmatter）

# 公式保真走 route B：纯文本 + 公式风险页整页 PNG，由 ingest 读图写 KaTeX。
# 不依赖任何重型 OCR/ML 后端（marker/surya 已评估，4GB 显存下太慢，弃用）。

# ── 测试 ─────────────────────────────────────────────────
pytest>=7.0

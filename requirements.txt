# PDF to Study KB 依赖
# 架构见 CLAUDE.md（Claude Code）/ AGENTS.md（Codex）
#
# 安装（建议专用 conda 环境，勿用共享环境）：
#   conda create -y -n study-kb python=3.12
#   conda activate study-kb
#   pip install -r requirements.txt

# ── 核心确定性 CLI（零 LLM 必需）──────────────────────────────
pymupdf>=1.23.0      # PDF 解析 / 页渲染（source-convert 文本后端 + 难页 PNG）
pyyaml>=6.0          # YAML（registry / workorder / frontmatter）

# 公式保真走 route B：纯文本 + 公式风险页整页 PNG，由 ingest 读图写 KaTeX。
# 核心 CLI 轻量（PyMuPDF 抽取）；PDF 严格验收另需 MinerU 作 structural reviewer（双审，见下方 + CLAUDE.md）。
# 旧 marker/surya 硬 OCR 管线已评估弃用（低显存 GPU 上太慢），勿重新引入（test_legacy_removed.py 守卫）。

# ── 测试 ─────────────────────────────────────────────────
pytest>=7.0

# ── MinerU 结构化后端（PDF 严格验收必需的 structural reviewer；为保持 dev 安装轻量，默认仍注释）──────
# `source-audit` 用 MinerU 复核每个 PDF 的 PyMuPDF 抽取（双审 → reconciliation.json）；strict / 生产验收
# 必需、未装即 fail-closed（非 strict dev 可不装，但产出标 degraded / 未双审、不算生产验收）。亦作扫描 /
# 低文本 PDF、DOCX / PPTX 的 primary 结构化解析（`source-convert --backend auto` 自动路由）。
# 仅用 MinerU 的 pipeline 后端（CLI 恒 `-b pipeline`，禁 vlm/hybrid），低显存 GPU（约 4GB 即可）。
#
# 推荐一键安装（按机型自动选 torch 构建，不固定 CPU 版本）：
#   python scripts/install_mineru.py            # 装 mineru[core] + 据 nvidia-smi 换匹配 CUDA torch；无 GPU 保留 CPU
#   python scripts/install_mineru.py --dry-run  # 先看将执行的命令
# 纯手动：`pip install mineru[core]`（只装 CPU torch；GPU 加速见 scripts/install_mineru.py）。
#
# 下面这行默认注释以保持 dev 安装轻量；生产 / strict 验收前请装 MinerU（取消注释或跑 install_mineru.py）：
# mineru[core]
